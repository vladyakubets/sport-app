import { Box } from "@mui/material";
import styled from "styled-components";

export const LeftBarBlock = styled(Box)`
  background-color: #151515;
  height: calc(100vh - 140px);
  overflow-y: scroll;
`;
